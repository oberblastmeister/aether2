(define (another) u64
  (block (start)
    (set one (const u64 1))
    (set two (const u64 2))
    (set res (call u64 (assert_eq_u64 one two)))
    (ret)
  )
)

(define (entry) u64
  (block (start)
    (set dummy (call u64 (another)))
    (ret)
  )
)
