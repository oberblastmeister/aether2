; (define (if [x : i64]) : i64
;   (block (start)
;     (set nine (const i64 9))
;     (set f (ucmp i64 gt x nine))
;     (cond_jump f (then) (else))
;   )
;   (block (then)
;     (set r (const i64 9))
;     (jump (done))
;   )
;   (block (else)
;     (jump (done))
;   )
;   (block (done)
;     (ret r)
;   )
; )
; 
(define (entry) : void
  (block (start)
;     (set res (call i64 (if (const i64 10))))
;     (call void (print_u64 res))
    (ret)
  )
)
