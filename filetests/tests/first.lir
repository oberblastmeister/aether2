(define (another) void
  (block (start)
    (set one (const u64 1))
    (set two (const u64 2))
    (call void (assert_eq_u64 one two))
    (ret)
  )
)

(define (entry) void
  (block (start)
    (call void (another))
    (ret)
  )
)
