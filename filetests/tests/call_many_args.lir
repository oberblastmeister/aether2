(define (f [a u64] [b u64] [c u64] [d u64] [e u64] [f u64] [g u64] [h u64] [i u64] [j u64] [k u64] [l u64] [m u64] [o u64]) void
  (block (start)
    (call void (print_u64 a))
    (call void (print_u64 b))
    (call void (print_u64 c))
    (call void (print_u64 d))
    (call void (print_u64 e))
    (call void (print_u64 f))
    (call void (print_u64 g))
    (call void (print_u64 h))
    (call void (print_u64 i))
    (call void (print_u64 j))
    (call void (print_u64 k))
    (call void (print_u64 l))
    (call void (print_u64 m))
    (call void (print_u64 o))
    (ret)
  )
)

(define (entry) void
  (block (start)
    (call void
      (f
        (const u64 0)
        (const u64 1)
        (const u64 2)
        (const u64 3)
        (const u64 4)
        (const u64 5)
        (const u64 6)
        (const u64 7)
        (const u64 8)
        (const u64 9)
        (const u64 10)
        (const u64 11)
        (const u64 12)
        (const u64 13)
      )
    )
    (ret)
  )
)
