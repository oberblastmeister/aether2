(define (entry) : void
  (block (start)
    (set x (const i64 2))
    (set zero (const i64 0))
    (set f (ucmp i64 gt x zero))
    (set value (const i64 0))
    (cond_jump f (then) (else))
  )
  (block (then)
    (set twenty (const i64 20))
    (set value (add i64 value twenty))
    (jump (done))
  )
  (block (else)
    (jump (done))
  )
  (block (done)
    (set zero (const i64 0))
    (set f (ucmp i64 gt x zero))
    (cond_jump f (then1) (else1))
  )
  (block (then1)
    (call void (print_u64 value))
    (jump (done1))
  )
  (block (else1)
    (jump (done1))
  )
  (block (done1)
    (ret)
  )
)
